---
name: three-minds
description: 三个能干活的 AI 分身协作系统。统一通过 claude-code-skill 调用所有模型（Claude/GPT/Gemini），共享同一个工作目录，多轮协作直到达成共识。适用于代码审查、方案评审、重构任务等需要多视角协作的场景。
version: 2.1.0
author: 莎莎大人
---

# Three Minds - 三个臭皮匠顶个诸葛亮

真正能干活的多 Agent 协作系统。不是纸上谈兵，而是真的能读文件、改代码、跑测试。

## 核心架构

```
任意模型 (Claude/GPT/Gemini/...)
           │
           ▼
    claude-code-skill CLI
           │
           ▼
      backend-api API
           │
           ▼
    Claude Code Agent Loop
    (tool use, file ops, code exec)
```

**所有模型统一走 claude-code-skill** —— 简单、一致、可靠。

## 使用方法

```bash
# 基础用法（默认 3 个 agent）
three-minds "审查并改进这个项目的代码质量" --dir ./my-project

# 使用代码审查配置
three-minds "审查 src/ 目录的所有代码" --config code-review --dir ./project

# 多模型协作：GPT + Gemini + Claude
three-minds "设计一个可扩展的 API" --config multi-model --dir ./project

# 指定最大轮数
three-minds "重构这个模块" --dir ./module --max-rounds 5
```

## 触发词

- "三个臭皮匠"
- "three minds"
- "多 agent 协作"
- "让三个 agent 一起审查"

## 预设配置

### default - 代码协作三人组
- 🏗️ **架构师** - 代码结构、设计模式、可扩展性
- ⚙️ **工程师** - 代码质量、错误处理、性能优化
- 🔍 **审核员** - 代码规范、潜在 bug、文档完整性

### multi-model ⭐ - 多模型协作组
配置示例：
```json
{
  "agents": [
    { "name": "GPT", "model": "gpt-4o", "baseUrl": "http://127.0.0.1:8082" },
    { "name": "Gemini", "model": "gemini-2.0-flash", "baseUrl": "http://127.0.0.1:8082" },
    { "name": "Claude", "model": "claude-sonnet-4-20250514" }
  ]
}
```

### code-review - 代码审查三人组
- 🛡️ **安全专家** - 安全漏洞、注入风险
- ⚡ **性能工程师** - 算法复杂度、内存优化
- ✅ **质量审查员** - 可读性、测试覆盖

## 自定义配置

```json
{
  "name": "我的团队",
  "agents": [
    {
      "name": "专家A",
      "emoji": "🎯",
      "model": "gpt-4o",
      "baseUrl": "http://127.0.0.1:8082",
      "persona": "你是一位专注于..."
    }
  ],
  "maxRounds": 10
}
```

## 依赖

- **必需**：`claude-code-skill` CLI
- **可选**：proxy 服务（如需使用非 Claude 模型）

## 相关项目

- [claude-code-skill](https://github.com/Enderfga/openclaw-claude-code-skill) - 核心依赖
